// add.model.ts
export interface Usuario {
    id: number;          // Identificador único del usuario
    nombre: string;     // Nombre del usuario del form
    correo: string;     // Correo electrónico del usuario
    edad: number;       // Edad del usuario
  }
  